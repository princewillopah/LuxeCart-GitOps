apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-headless
  namespace: {{ .Values.namespace }}
  labels:
    app: elasticsearch

spec:
  clusterIP: None
  selector:
    app: elasticsearch
  ports:
    - name: http
      port: 9200
      targetPort: 9200