apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: {{ .Values.namespace }}
  labels:
    app: mongodb
    tier: data

spec:
  type: ClusterIP

  selector:
    app: mongodb

  ports:
    - name: mongo
      port: 27017
      targetPort: 27017